# ── Web framework ──
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
pydantic-settings==2.5.0

# ── Database ──
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.0
greenlet==3.1.0

# ── Auth ──
PyJWT==2.9.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0

# ── HTTP client (for pipeline trawler) ──
aiohttp==3.10.0

# ── CORS ──
# (included in fastapi)

# ── Dev ──
httpx==0.27.0
pytest==8.3.0
pytest-asyncio==0.24.0
